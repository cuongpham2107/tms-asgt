# Fix: Mobile + Backend hiển thị Cancelled status

## Backend: 2 file

### 1. Lọc Cancelled khỏi shift API
**File:** `app/Http/Controllers/Api/ShiftStatusController.php:39`
```php
// Before:
$activeShift->load(['driver', 'trips.vehicle'])
// After:
$activeShift->load(['driver', 'trips' => fn($q) => $q->where('status', '!=', 'cancelled'), 'vehicle'])
```

### 2. Lọc Cancelled khỏi shift current API
**File:** `app/Http/Controllers/Api/DriverShiftController.php:295`
```php
// Before:
$shift->load(['driver', 'trips.vehicle'])
// After:
$shift->load(['driver', 'trips' => fn($q) => $q->where('status', '!=', 'cancelled'), 'vehicle'])
```

## Mobile: 4 file

### 3. `mobile/app/(tabs)/trips.tsx`
- **statusConfig** (line 8-18): Thêm `cancelled: { icon: "close-circle", bg: "#FEE2E2", text: "#DC2626", label: "Huỷ" }`
- **Tabs** (line 20-27): Thêm `{ key: "cancelled", label: "Huỷ", color: "#DC2626" }`
- **Order color** (line 176): Thêm `o.status === "cancelled" ? "#DC2626" :`

### 4. `mobile/app/trip-detail.tsx`
- **statusConfig** (line 11-21): Thêm `cancelled: { icon: "close-circle", bg: "#FEE2E2", text: "#DC2626", label: "Đã huỷ" }`
- **orderStatusLabel** (line 23-25): Thêm `cancelled: "Huỷ"`

### 5. `mobile/app/order-detail.tsx`
- **statusConfig** (line 25-59): Thêm `cancelled: { icon: "close-circle", bg: "#FEE2E2", text: "#DC2626", label: "Huỷ" }`
- **cpInfo** (line 61-81): Thêm `cancelled: { icon: "close-circle", color: "#DC2626", label: "Huỷ chuyến" }`

### 6. `mobile/app/(tabs)/profile.tsx`
- **activeTrips filter** (line 22): Thêm `t.status !== "cancelled"`

## Verification
1. Mobile trips screen: hiển thị label "Huỷ" màu đỏ cho trip Cancelled
2. Mobile trip detail: hiển thị status "Đã huỷ" + order status "Huỷ"
3. Mobile order detail: hiển thị status "Huỷ" + checkpoint "Huỷ chuyến"
4. Profile: nút "Kết thúc ca" không bị block bởi trip Cancelled
5. Shift API: không trả về trip Cancelled trong danh sách trips của ca
6. `php artisan test --compact`
