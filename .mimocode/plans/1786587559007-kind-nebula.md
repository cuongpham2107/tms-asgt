# Kiểm tra dữ liệu chuyến CD-2026-08-13-207 trả về cho từng lái xe (mobile)

## Kết luận: Dữ liệu ĐÃ ĐÚNG — không cần sửa code

Trip 207 đã được kiểm chứng end-to-end (read-only): API `GET /api/driver/trips/{id}`
giờ trả về km riêng cho từng tài xế đúng với thực tế chạy, nhờ fix trước đó
(`TripKmSplitService` + `TripResource::adjustedKmForDriver`).

## Dữ liệu trip 207

- Trạng thái: `completed`, tài xế cuối: 160 (Vũ Duy Huy), shift 169
- `start_km=101110`, `end_km=101250`, `total=140`, `loaded=100`, `empty=40`
- 3 tài xế tham gia, 8 lần đảo lái: 109 → 107 → 160 → 107 → 160 → 107 → 160 → 107 → 160

### Kết quả API trả về cho từng tài xế (đã mô phỏng qua TripResource thật)

| Tài xế | total_km | loaded | empty | Khớp checkpoint |
|---|---|---|---|---|
| 109 — Hà Thanh Dóng | 30 | 0 | 30 | 101110→101140 |
| 107 — Chu Văn Toàn | 70 | 70 | 0 | 101140→101170, 101180→101190, 101200→101210, 101220→101240 |
| 160 — Vũ Duy Huy | 40 | 30 | 10 | 101170→101180, 101190→101200, 101210→101220, 101240→101250 |

Kiểm tra chéo:
- Σ total = 30+70+40 = **140** = trip total ✓
- Σ loaded = 0+70+30 = **100** = trip loaded ✓

`TripKmSplitService::segments()` tách đúng 9 đoạn, `loadedRanges()` = [101140, 101240]
(khoảng có hàng từ `arrived_pickup` tới `completed`) khớp hoàn toàn.

## Ghi chú (không bắt buộc xử lý)

- Swap id 96, 97 vẫn có `handover_km = NULL` (tạo trước khi fix `ReassignDriverAction`).
  Không ảnh hưởng vì `HandoverKmResolver` đã fallback về checkpoint `driver_swap`
  (96 → 101140, 97 → 101170) đúng.
- Các swap mới (98–105) đã có `handover_km` → xác nhận fix `ReassignDriverAction`
  đang hoạt động.

## Hành động

Không cần thay đổi code. Chỉ báo cáo kết quả kiểm chứng cho người dùng.
