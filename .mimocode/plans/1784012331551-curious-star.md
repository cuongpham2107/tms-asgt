# Fix: vehicle_id không được lưu vào trip_checkpoints

## Nguyên nhân
`TripCheckpoint` model `$fillable` **thiếu `vehicle_id`**. Khi `CheckpointFactory::buildData()` trả về mảng có `vehicle_id`, Eloquent mass assignment protection tự động bỏ qua trường này → lưu vào DB luôn là NULL.

## Fix: 1 dòng
File: `app/Models/TripCheckpoint.php`
Thêm `'vehicle_id',` vào `$fillable` array.

## Verify
- Tạo checkpoint mới qua API hoặc tinker
- Check DB: `vehicle_id` có giá trị (không null)
